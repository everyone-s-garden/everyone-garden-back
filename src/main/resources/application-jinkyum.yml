server:
  port: 80

  error:
    include-message: always

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/garden
    username: root
    password: bookitout

  jpa:
    showSql: true
    hibernate:
      ddlAuto: update
      format-sql: true